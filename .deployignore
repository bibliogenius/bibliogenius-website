.git/
.gitignore
.claude/
.DS_Store
.deployignore
Makefile
README.md
LICENSE
requirements.txt

# Self-hosted binaries (uploaded out-of-band by `make ship-linux`).
# Excluded so the site's --delete rsync never wipes them.
downloads/

# GitHub Pages markers (unused on Caddy)
.nojekyll
CNAME

_blog/
_build/
_changelog/
_docs/
_i18n/
